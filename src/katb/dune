(ocamllex (modules lexer))
(menhir (modules menhir_parser))

(library
  (name katblib)
  (public_name netkat.katblib)
  (wrapped true)
  (libraries
    base
    netkat
    idds
    parsing
  )
  (preprocess (pps
    ppx_jane
  ))
)
